/*
** EPITECH PROJECT, 2024
** create_player_one.c
** File description:
** create_player_one.c
*/

#include "my.h"

static int handle_arg(FILE *fd)
{
    if (!fd)
        return 84;

    return 0;
}

int create_creating_player(char *file_path)
{
    FILE *fd = fopen(file_path, "r");

    return 0;
}
